# GEMINI.md

## 💡 개발 원칙 (Core Principles)

1. **Language**
- 모든 응답과 설명은 **한국어**로 진행한다.
- 기술 용어(Technical Terms)나 고유 명사는 필요 시 **영어**를 사용할 수 있다.

2. **Context First**
- 코드 수정 전 `types/`, `lib/`, 공통 유틸, 매핑 테이블, 기존 훅 구조를 먼저 확인하여 일관성을 유지한다.
- 기존 프로젝트의 데이터 흐름과 공통 규칙을 확인하지 않은 상태에서 임의로 새 구조를 만들지 않는다.

3. **Intent Alignment**
- 사용자의 질문(Inquiry)과 명령(Directive)을 엄격히 구분한다.
- 상태 확인, 에러 로그 공유, 코드 공유, 중간 결과 공유, “이게 맞아?” 같은 질문은 작업 승인으로 간주하지 않는다.
- 모호한 경우 반드시 확인한다.
  - 예: `계획만 드릴까요, 바로 수정할까요?`

4. **No Autonomous Action**
- 사용자가 지시하지 않은 작업을 “효율적”이라고 판단하여 임의로 수행하지 않는다.
- 명시적 승인 없이 코드를 먼저 수정하지 않는다.
- 모든 실행은 대화와 협의를 통해 확정된 사항만 수행한다.

5. **Preservation**
- 기존에 구현된 기능, UI 레이아웃, 동작 흐름을 자의적으로 삭제하거나 변경하지 않는다.
- 변경이 필요한 경우 반드시 사용자에게 먼저 제안하고 승인을 받는다.

6. **Feature Preservation**
- 새로운 기능을 추가하거나 기존 기능을 수정할 때, 기존에 잘 작동하던 다른 기능들을 임의로 수정하거나 누락하지 않는다.
- 변경 작업 전 영향 범위를 먼저 파악하고 기존 기능의 무결성을 유지한다.

7. **Minimalism**
- 불필요한 코드, 불필요한 주석, 불필요한 추상화를 지양한다.
- 구조 개선은 “지금 필요한 수준”까지만 수행한다.
- 동작 안정성이 우선이며, 보기 좋은 리팩토링보다 버그 방지가 우선이다.

8. **Stable Data Flow**
- 함수 이름, 매개변수(Parameters), 데이터 구조, import/export를 변경할 때는 반드시 프로젝트 전체 호출부를 먼저 조사하고 일괄 반영한다.
- 부분 수정으로 호출부가 깨지는 상황을 만들지 않는다.

9. **Event Integrity**
- 키보드, 클립보드, 포커스, 선택, 드래그 관련 이벤트 로직을 수정할 때는 우선순위 계층을 절대 보존한다.
- 기본 우선순위:
  1. Modal
  2. Input Editing
  3. Active Cell
  4. Selection Range
  5. Global Shortcut

10. **Modesty in Communication**
- “완벽”, “절대”, “100% 보장”, “완전히 해결” 같은 과도한 확신 표현을 사용하지 않는다.
- 진행 상태, 변경 범위, 남아 있는 위험 요소를 사실 중심으로 보고한다.

---

## 🔒 실행 통제 규칙 (Execution Control Rules)

### 1. 승인 전 수정 금지 (No Edit Before Approval)
- 사용자가 명시적으로 승인하기 전에는 어떤 파일도 수정하지 않는다.
- `계획 설명`과 `실제 코드 수정`은 반드시 분리한다.
- 아래 표현이 없는 한 수정 승인이 아니다:
  - `수정해줘`
  - `적용해줘`
  - `진행해줘`
  - `승인`
- 승인 전에는 다음 작업을 금지한다:
  - 파일 생성
  - 파일 수정
  - 함수명 변경
  - import/export 수정
  - 구조 이동
  - 리팩토링 실행

### 2. 계획 우선 원칙 (Plan First Rule)
- 수정이 필요할 경우 반드시 아래 순서로 진행한다:
  1. 현재 코드 분석
  2. 파일 단위 수정 계획 제시
  3. 영향 범위 설명
  4. 사용자 승인
  5. 실제 수정
- 계획에는 반드시 아래를 포함한다:
  - 어떤 파일을 수정할지
  - 어떤 함수/훅/유틸을 분리할지
  - 구조 분리인지 / 동작 변경인지
  - 위험 가능성이 있는 부분
  - 테스트가 필요한 부분

### 3. 부분 승인 원칙
- 사용자가 일부 파일이나 일부 작업만 승인한 경우, 승인 범위를 절대 넘기지 않는다.
- 승인되지 않은 파일이나 관련 있어 보이는 로직도 임의로 함께 수정하지 않는다.
- 예:
  - `clipboardParser.ts`만 승인 → 그것만 수정
  - `selection/drag 보류` → 관련 코드는 수정 금지

### 4. 구조 분리와 동작 변경 분리
- 리팩토링 시 반드시 아래 둘을 구분해서 설명한다:
  - **구조 분리**: 파일 이동, 함수 추출, 유틸 분리, import 정리
  - **동작 변경**: 조건문 변경, 이벤트 흐름 변경, 대상 범위 변경, 상태 갱신 순서 변경
- 사용자가 “구조만 분리”를 요청한 경우, 동작 변경은 금지한다.
- 구조 분리 과정에서 동작 변경이 불가피하다면 반드시 사전에 이유와 범위를 설명하고 승인받는다.

---

## 🛠 코드 품질 및 유지보수 규칙 (Code Quality Rules)

### 1. 파일 및 구조 분리 기준 (Mandatory Separation)
- **파일 크기**: 단일 파일이 **300줄**을 초과하면 반드시 논리적 단위로 분리한 후 작업을 진행한다.
- **함수/훅 크기**: 단일 함수나 커스텀 훅이 **50줄**을 초과하면 로직을 쪼개어 분리한다.
- **useEffect 관리**: 한 컴포넌트 내에 `useEffect`가 **3개 이상** 존재하면 반드시 커스텀 훅으로 추출한다.
- **Props 관리**: 컴포넌트의 `props`가 **5개 이상**이면 객체로 묶거나 컴포넌트 설계를 재검토한다.

### 2. 저장 로직 규칙 (Save Logic Standards)
- 저장 관련 코드는 반드시 **한 곳(단일 함수/훅)**에만 작성한다.
- 복잡한 저장 로직은 반드시 전용 커스텀 훅(예: `useStudentSave`)으로 분리한다.
- `onBlur`, `onKeyDown` 저장 시 반드시 `e.target.value`를 직접 전달한다.
  - 올바른 패턴: `onBlur={(e) => onSave(colId, e.target.value)}`
  - 잘못된 패턴: `onBlur={() => onSave()}` 후 내부에서 `ref`를 읽는 방식
- `blur` 시점에는 컴포넌트 언마운트로 인해 `ref.current`가 `null`이 될 수 있음을 항상 고려한다.

### 3. 데이터 흐름 규칙 (Single Source of Truth)
- 부모 컴포넌트는 데이터를 소유하고(State), 자식은 이벤트를 통해서만 변경을 요청한다.
- 저장 성공 후 전체 데이터를 **Refetch(fetchAllData)** 하지 않는다.
- 올바른 패턴:
  ```ts
  setStudents(prev => prev.map(s => s.id === id ? { ...s, ...savedData } : s))
  ```
- `useEffect`로 외부 데이터 → 로컬 상태를 동기화할 때는 사용자가 **입력 중(isUserEditing)** 이거나 **저장 중(isSaving)** 이면 업데이트를 건너뛴다.

### 4. 자동저장(디바운스) 사용 금지
- 입력 중 실행되는 자동저장 타이머(디바운스)는 사용하지 않는다.
- 저장은 오직 아래 두 경우에만 실행한다:
  1. **Enter** 또는 **Tab**
  2. 셀에서 포커스가 완전히 나갈 때 (**onBlur**)

### 5. 하드코딩 금지 (No Hardcoded Lists)
- 컬럼 ID, DB 필드명, 저장 대상 목록, 삭제 대상 목록을 하드코딩 배열로 직접 관리하지 않는다.
- 반드시 중앙 매핑 테이블 또는 공통 상수를 우선 사용한다.
  - 예: `COLUMN_TO_FIELD_MAP`, `FIELD_TO_COLUMN_MAP`
- 새로운 컬럼이 추가될 가능성이 있는 로직은 매핑 기반으로 작성한다.

### 6. 공용 유틸 책임 분리 (Single Responsibility)
- 공용 유틸 함수는 하나의 행동만 책임지게 작성한다.
- 붙여넣기용 DOM 동기화와 삭제용 DOM 동기화는 동일하지 않을 수 있으므로 필요 시 분리한다.
- 하나의 공용 유틸에서 여러 행동을 뭉뚱그려 처리하지 않는다.

---

## 🧭 TodaySheet 안전 규칙 (TodaySheet Safety Rules)

### 1. Selection / Drag 보호 규칙
- `TodaySheet` 작업 시 selection 관련 상태와 drag 상태 로직은 명시적으로 요청된 경우에만 수정한다.
- 구조 분리 작업 중에는 아래 항목을 임의로 변경하지 않는다:
  - `selectedRange`
  - `activeCell`
  - `editingCell`
  - `isDragging`
  - 전역 `mousedown`, `mouseup`, `click`, `keydown` 이벤트 바인딩
- selection/drag 관련 코드를 수정해야 할 경우, 반드시 먼저 diff 기준 계획을 보여주고 승인을 받는다.
- 붙여넣기/삭제 안정화 작업과 selection/drag 수정은 한 번에 같이 진행하지 않는다.

### 2. 클립보드 / 삭제 로직 분리
- 붙여넣기(paste)와 삭제(clear/delete)는 비슷해 보여도 같은 동작으로 취급하지 않는다.
- 두 로직의 대상 범위, DOM 반영 방식, 상태 업데이트 순서를 분리해서 검토한다.
- 공용화는 “동작 동일성”이 검증된 뒤에만 허용한다.

### 3. DOM Sync 안전 규칙
- React state와 실제 DOM을 함께 다룰 경우, 어떤 시점에 어떤 값을 반영하는지 명확히 설명할 수 있어야 한다.
- DOM sync는 반드시 “어떤 셀을 왜 업데이트하는지” 추적 가능해야 한다.
- `Object.keys(newData)`처럼 데이터 전체를 순회하여 DOM을 건드리는 방식은 기본적으로 금지한다.
- 삭제 작업에서는 실제로 지운 컬럼 ID 목록(`targetColIds`)만 대상으로 DOM을 갱신한다.
- 붙여넣기 작업에서는 실제로 붙여넣은 컬럼 ID 목록만 대상으로 DOM을 갱신한다.

### 4. 타이밍 패치 규칙
- `setTimeout` 기반의 임시 타이밍 패치는 새로 추가하지 않는다.
- 타이밍 조정이 필요할 경우 원칙적으로 `requestAnimationFrame`을 우선 사용한다.
- 단, `requestAnimationFrame` 도입 시에도 기존보다 지연이 증가하지 않는지 확인해야 한다.
- 타이밍 패치는 근본 해결이 아니라 보조 수단으로만 사용한다.

### 5. TodaySheet 변경 후 필수 검증
- `TodaySheet` 관련 코드를 수정한 후에는 보고 전 반드시 아래를 자가 점검한다:
  - 셀 선택(파란 테두리)이 안정적으로 유지되는가?
  - drag 후 selection 범위가 깨지지 않는가?
  - Cmd+Z가 작동하는가?
  - 방향키 / Enter / Tab 네비게이션이 유지되는가?
  - 붙여넣기 후 첫 번째 셀이 즉시 보이는가?
  - 멀티셀 삭제 후 첫 번째 셀이 늦게 지워지거나 다시 나타나지 않는가?
  - 저장 후 전체 refetch 없이 로컬 상태만 정확히 갱신되는가?
  - 저장 시 화면 멈춤이나 지연(Save Lag)이 없는가?

---

## 🧪 디버깅 및 분석 규칙 (Debugging & Analysis Rules)

### 1. 근본 원인 우선
- 동일한 버그가 2회 이상 반복되면 임시 패치를 덧대지 말고 구조적 원인을 먼저 분석한다.
- 다음 중 무엇이 문제인지 먼저 분리해서 판단한다:
  - 상태 동기화 문제
  - DOM 반영 시점 문제
  - 이벤트 우선순위 문제
  - 전체 refetch / 외부 동기화 문제
  - 비제어 컴포넌트(`defaultValue`) 구조 문제

### 2. 데이터 검증 우선
- 원인이 불분명할 경우 추측으로 수정하지 않는다.
- `console.log` 또는 동등한 방법으로 실제 이벤트 타이밍, state 값, DOM 값, 저장 요청 시점을 확인한 뒤 수정한다.

### 3. 언마운트 주의
- `ref` 값이 예상과 다르게 `null`이거나 비어 있을 때는 항상 컴포넌트 언마운트 또는 리렌더 타이밍을 먼저 의심한다.

### 4. 낙관적 업데이트 우선 검토
- 삭제, 붙여넣기, 저장 후 깜빡임 문제는 DOM 패치만으로 해결하려 하지 말고 로컬 상태의 낙관적 업데이트 가능성을 먼저 검토한다.
- DB 저장 완료를 기다리기 전에 로컬 상태를 먼저 반영할 수 있는지 판단한다.

---

## 📋 수정 후 보고 규칙 (Reporting Rules)

### 1. 보고 형식 고정
- 수정 후에는 반드시 아래 형식으로 보고한다:
  1. 수정한 파일 목록
  2. 구조 분리인지 / 동작 변경인지 구분
  3. 기존 동작과 달라진 점
  4. 남아 있는 위험 가능성
  5. 테스트가 필요한 시나리오

### 2. 과장 표현 금지
- 아래 표현은 사용하지 않는다:
  - `완벽`
  - `절대`
  - `100% 보장`
  - `완전히 해결`
  - `문제 없음 확정`
- 대신 다음처럼 표현한다:
  - `현재 기준으로 위험이 줄었습니다`
  - `이 부분은 테스트가 더 필요합니다`
  - `동작 동일성은 확인이 필요합니다`

---

## 🚀 새 기능 추가 전 체크리스트

- [ ] 현재 파일이 300줄 이상인가? → **먼저 분리**
- [ ] 함수/훅이 50줄 이상인가? → **먼저 분리**
- [ ] 저장 로직이 여러 곳에 흩어져 있는가? → **먼저 통합**
- [ ] 부모가 저장 후 전체 리페치를 수행하는가? → **먼저 개별 업데이트(Local state) 방식으로 변경**
- [ ] 이번 작업이 구조 분리인가, 동작 변경인가? → **먼저 구분**
- [ ] 사용자의 명시적 승인을 받았는가? → **승인 전 수정 금지**
- [ ] selection/drag 관련 코드가 포함되는가? → **별도 승인 필요**
- [ ] 하드코딩 리스트를 쓰고 있는가? → **매핑 기반으로 변경**
- [ ] DOM sync가 전체 필드를 순회하는가? → **대상 컬럼만 갱신하도록 수정**
- 사용자가 “계획만”, “분석만”, “diff만”이라고 명시한 경우, 수정 요청으로 해석하지 않는다.