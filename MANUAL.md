# 📝 호크마 학원 플래너 — TodaySheet 실무 사용 설명서 (User Manual)

---

> [!IMPORTANT]
> **본 설명서는 매일 학원 현장에서 수업을 직접 정비하시는 원장님과 강사 선생님들을 위한 실전 지침서입니다.**
> TodaySheet(데일리시트)에 배치된 핵심 툴박스 아이콘들의 용도와 작동 방식을 완벽하게 숙지하시어, 낭비되는 행정 업무 시간을 제로(0)로 단축해 보세요!

---

## 🛠️ 1. 셀(Cell)별 우측 미니 툴박스 아이콘 가이드

각 학생의 이름 칸이나 입력 칸 근처에 마우스를 올리거나 셀을 선택하면 나타나는 **미니 조작 단추**들의 역할입니다.

| 아이콘 / 심볼 | 이름 | 추천 색상 | 실무 동작 및 최적의 활용 시점 |
| :---: | :---: | :---: | :--- |
| <span style="color: #e11d48; font-weight: 900; border: 1px solid #fda4af; padding: 2px 6px; border-radius: 4px; background: #ffe4e6; font-size: 11px;">R</span> | **Reset & Remove**<br>(리셋 및 보강 제외) | **카민 로즈 🔴** | **"오늘 하루의 낙서와 실수를 초기화하고 싶을 때" 사용합니다.**<br>• **정규 수업생에게 누를 때 (`Reset`)**: 오늘 잘못 적은 수행진도, 숙제, 테스트 성적 등을 DB에서 즉시 영구 삭제하고 아침 출근 직후의 백지상태인 **`수업전 ⚪`**으로 리셋합니다.<br>• **보강 학생(비요일생)에게 누를 때 (`Remove`)**: 오늘 보강 등원 기록 자체를 완벽히 포맷하여, 오늘 명단에서 흔적도 없이 깔끔하게 제외합니다. |
| 📝 | **출결 사유 입력**<br>(연필 단추) | **단정한 연필 ✏️** | **"결석생의 오늘 사유를 기록하고 수업 일지를 수호할 때" 씁니다.**<br>• 클릭 후 사유(예: `시험기간`, `독감`, `가족여행` 등)를 치면, 오늘 하루 누적 결석 통계에 즉시 자동 반영됩니다.<br>• **[스마트 보호]**: 수행진도 칸에 기존 선생님이 적어둔 수업 기록이 있다면 지우지 않고, 맨 윗줄에 정중하게 `[결석] 독감` 형태로 **자동 덧붙임(Prepend)** 됩니다. |
| ⏳ | **과거 이력 소환**<br>(History) | **오션 블루 🔵** | **"이 학생이 저번 시간에 어디까지 나갔지? 숙제는 해왔었나?" 싶을 때 씁니다.**<br>• 클릭 즉시 해당 학생의 지난 3개월간의 수업 일지(진도, 교재, 숙제 수행도)가 화면 우측에 슬라이드 팝업으로 전격 소환되어 비교 정비할 수 있습니다. |
| 🖨️ | **호크마 일지 인쇄**<br>(Journal Print) | **클래식 차콜 ⚫** | **"학부모 보고용 고급 지면 일지를 출력하고 싶을 때" 씁니다.**<br>• 오늘 적은 모든 진도와 수행 성적, 숙제 점수가 가득 담긴 **미리보기 일지 창**이 열립니다.<br>• **[실시간 연동]**: 새로고침을 할 필요 없이, 대시보드에 글을 치는 즉시 인쇄 화면에 100% 동기화 반영됩니다. |
| ⚙️ | **툴바 편집 조절**<br>(Settings) | **메탈릭 그레이 ⚙️** | **"자주 쓰는 버튼만 상시 노출하고 싶을 때" 사용합니다.**<br>• 톱니바퀴 단추를 누르면, 아이콘들 사이에 노란 경계선(`separator` 🟡)이 나타않고 단추들의 배열이 보입니다.<br>• 이 경계선을 드래그 앤 드롭하여 내 손맛에 딱 맞게 **상시 노출할 단추의 개수**를 조절하고, 나머지는 더보기(`...`) 창 안으로 안전하게 숨길 수 있습니다. |

---

## 🟢 2. 원형 출결 상태 아이콘 가이드

학생 이름 바로 왼쪽에 위치한 동그라미 단추로, 클릭할 때마다 출결 상태가 순차적으로 순환 변경되며 데이터베이스에 실시간 기록됩니다.

* **⚪ 수업전 (미등원)**
  - 아직 등원 처리나 결석 체크를 진행하지 않은 깨끗한 백지 상태입니다.
* **🟢 출석 (수업개시)**
  - 학생이 등원하여 오늘 수업을 힘차게 시작했음을 뜻합니다.
* **🟡 지각 (지각등원)**
  - 약속된 등원 타임보다 지각하여 입실했으나 출석과 동일하게 취급되어 출석 통계에 정상 가산됩니다.
* **🔴 결석 (등원취소)**
  - 오늘 학원에 등원하지 않았음을 정식 기록합니다. 이 상태로 바뀌면 옆에 **연필 📝 단추**가 자동으로 등장하여 사유를 빠르게 기입할 수 있게 유도합니다.
* **❌ 수업제외 (면제)**
  - 공휴일, 공식 휴가, 장기 요양 등으로 인해 오늘 정규 등원 명단에서 공식적으로 면제되었음을 뜻합니다.

---

## 🔄 3. 순환형(Cyclic) 스마트 버튼 가이드

단 1개의 버튼으로 클릭할 때마다 순환 작동하여 툴바 레이아웃 밀림(덜컹거림)을 원천 차단하고 업무 시간을 극대화하는 스마트 단축 조작 가이드입니다.

| 순환 버튼 | 클릭 순서 및 작동 상태 | 추천 색상 / 표현 | 실무 활용 팁 |
| :---: | :--- | :---: | :--- |
| **시트 헤더 체크박스**<br>(4단계 순환) | **[ ] (전체 해제)** ➔ **[✓] (전체 선택)** ➔ **[✓] (선택과목만)** ➔ **[✓] (정규수업만)** ➔ **[ ] (전체 해제)** | 파란색 ➔ 주황색 ➔ 청록색 | 헤더 맨 좌측 체크박스 1개로 시트 내 전체/선택과목/정규수업 학생들을 원하는 대로 순환 선택 및 일괄 해제합니다. |
| **Sort 정렬 통합 버튼**<br>(4단계 순환) | **⏰ 시간순** ➔ **🔤 이름순** ➔ **🎓 학년순** ➔ **🏫 학교순** | 오션 블루 🔵 | 뚱뚱하던 4개 정렬 버튼을 1개로 통합하여 툴바 1줄 고정을 유지하고, 시트가 밑으로 밀리는 덜컹거림 버그를 100% 차단했습니다. (옆의 `▲/▼` 버튼으로 오름/내림차순 유지) |
| **보강 모달 과목 토글** | **[정규만]** ↔ **[선택과목만]** (상호 배타적) | 주황(호박) 🟠 | 보강 일정 예약 시 정규수업과 선택과목(특강) 수강생을 실수 없이 깔끔하게 분리하여 검색/선택합니다. |

---

## 🎛️ 3. 하단 툴바 전역 제어 아이콘 가이드

TodaySheet 하단 바에 위치한 강력한 전역 통제 도구들의 사용법입니다.

### 🔄 A. 3단 순환형 출결 필터 (전체 ➔ 출석 ➔ 결석)
원장님의 날카로운 실무 아이디어로 고안된 명품 필터 버튼입니다. 누를 때마다 화면의 명단이 기분 좋게 슥슥 걸러집니다.

* **👁️ 전체 (그레이 테마)**
  - 오늘 등원하기로 약속된 정규반, 보강반, 결석생 등 **모든 학생들을 한눈에 노출**합니다.
* **🟢 출석 (에메랄드 테마)**
  - 오늘 실제로 등원하여 수업 중인 **[출석/지각] 학생들만 정제**하여 보여줍니다. 수업 중 집중 관리에 탁월합니다.
* **🔴 결석 (로즈 테마)**
  - 오늘 등원하지 못한 **[결석] 학생들만 한곳에 압축**해서 봅니다. 결석생 사유 정비 및 학부모 안부 전화를 빠르게 돌릴 때 눈부신 위력을 발휘합니다.
  - *Tip: 결석 필터를 켠 뒤 모여 있는 소수 학생들의 연필 📝 단추만 톡톡 눌러 사유를 정비하면 단 10초 만에 결석 정리가 끝납니다!*

### 🪄 B. 태그입력 (Tag Batch Input)
* **아이콘**: 요술봉 🪄 모양
* **용도**: 여러 명의 학생에게 **동일한 교재, 진도, 숙제, 시험 범위**를 동시에 입력해 주고 싶을 때 사용합니다.
* **사용법**: 
  1. 명단 좌측의 체크박스로 대상 학생들을 멀티 선택합니다.
  2. 하단의 **`태그입력`** 단추를 누릅니다.
  3. 교재나 오늘 숙제 텍스트를 적고 저장하면, 선택된 학생 전원의 칸에 데이터가 한꺼번에 순식간에 보급됩니다.

---

> [!TIP]
> **마지막 상태 기억 기능**: 
> 내가 설정해 둔 하단 툴박스의 상시 노출 개수(`⚙️`)와 출결 필터(`전체`/`출석`/`결석`) 상태는 브라우저가 영구적으로 기억합니다. 내일 아침 다시 플래너를 켜도 원장님이 설정해 두신 최적의 레이아웃이 무결하게 복원됩니다!

---

## 📊 4. 신규/기존 학생 엑셀 일괄 등록 가이드

학원을 처음 세팅하거나 신학기 명단을 한 번에 수십~수백 명씩 등록할 때 활용하는 **엑셀 일괄 등록 지침**입니다.

### 📥 A. 양식 다운로드 및 작성 순서
1. 화면 상단 **[학생 관리]** ➔ **[엑셀 일괄 등록]** 클릭 후 **`[양식 다운로드]`** 단추를 누릅니다.
2. 다운로드받은 `학생일괄등록_양식.xlsx` 파일을 엽니다.
   - **1행 (헤더)**: `이름`, `학년`, `학교`, `반명`, `학생연락처`, `부모연락처`, `코스`, `수업요일`, `수업시작시간`, `수업시간(시수)`, `담당교사`
   - **2행 (예시 행)**: `홍길동`, `초5`, `호크마초등학교`, `삼산-Y`, `010-1234-5678`, `010-9876-5432`, `C`, `월수금`, `19:00`, `3`, `윤여태`
3. **[주의]** 2행의 예시 데이터('홍길동')는 지우시거나 실제 학생 데이터로 수정하여 작성합니다.

### 💡 B. 주요 컬럼 실무 팁
* **`이름` (필수)**: 학생 이름이 없는 행은 등록에서 자동으로 제외됩니다.
* **`수업요일` (강력 권장)**: `월수금`, `화목`, `월화수목금` 형태로 적어주시면 주간 등원 요일이 자동 계산되어 오늘 일지 표에 해당 요일 학생으로 자동 노출됩니다.
* **`수업시작시간` (시작 시각 파싱)**: `19:00`, `7시`, `16:00` 형태로 입력하면 등원 시작 시각이 자동 파싱됩니다. (미입력 시 기본 16:00 시작)
* **`수업시간(시수)` (수업 시간 계산)**: `3` (또는 `3시간`, `2` 등)으로 입력하면 **19:00 ~ 22:00**처럼 종료 시각까지 완벽히 계산되어 스케줄에 반영됩니다. (미입력 시 기본 3시간)
* **`부모연락처` (스마트 기능)**: 입력 시 부모님 전화번호 **뒷 4자리가 학부모 전용 로그인 비밀번호(`login_suffix`)로 자동 설정**됩니다.
* **`담당교사`**: 등록된 선생님 이름을 적으면 담당 선생님이 자동으로 연결됩니다.
* **`학년`/`학교`/`코스`**: 미입력 시 기본값(`미지정`, 코스 `C`)으로 자동 입력되므로 안 적으셔도 무방합니다.

